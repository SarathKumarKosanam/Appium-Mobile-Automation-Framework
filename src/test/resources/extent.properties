extent.reporter.spark.start=true
extent.reporter.spark.out=test-output/SparkReport/Index.html

# The "Secret" lines for screenshots
screenshot.directory=test-output/
screenshot.relativetpath=true

# Add this to embed images directly in the HTML (Easiest for sharing)
extent.reporter.spark.base64imagesrc=true